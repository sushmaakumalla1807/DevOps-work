# docker-compose.yml
# version: '3'
services:
  backend:
    build: ./backend
    image: backend
    ports:
      - "5000:5000"
    # depends_on:
    #   - mongo
  
  frontend:
    build: ./frontend
    image: frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
  
#   mongo:
#     image: mongo:latest
#     container_name: mongodb
#     ports:
#       - "27017:27017"
#     volumes:
#       - mongo-data:/data/db

# volumes:
#   mongo-data:
#     driver: local